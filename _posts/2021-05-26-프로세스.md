---
layout: post
title:  "메모리구조"
summary: "메모리구조"
author: GH
date: '2021-05-21 14:35:23 +0530'
category: 프로그래밍 기초
thumbnail: C:\Users\home\Desktop\공부저장\pcb.png
keywords: devlopr jekyll, how to use devlopr, devlopr, how to use devlopr-jekyll, devlopr-jekyll tutorial,best jekyll themes
permalink: /blog/categories/프로그래밍 기초/
usemathjax: true

---

# 프로세스(Process)

프로세스란 메인 메모리에 할당되어 <b>실행중인 상태인 프로그램 </b>을 말한다.

ex)  문서나 카카오톡 같은 실행파일을 설치하면 하드디스크에 저장이 되고
실행하지 않으면 디스크에 존재하기만 합니다. 하지만 실행을 하게되면 프로세스가 되는것입니다.

프로그램이란?
프로그램은 보조기억장치(하드디스크)에 저장되어 실행되기를 기다리는 코드,데이터의 정적인 묶음



## 프로세스의 구성

프로세스에 대한 정보는 <b>PCB(Process Control Block)</b>이라고 부르는 자료구조에 저장이되며, 프로세스가 생성될때 같이 생성됩니다.

![](C:\Users\home\Desktop\공부저장\pcb.png)

1. PID : 프로세스 식별자
   * 운영체제에서 각각의 프로세스를 구별하기 위한 프로세스 식별번호
2. 프로그램 상태
   + CPU는 프로세스를 빠른 속도로 교체하며 여러 프로세스를 실행하게 되는데 이때 상태를 의미 합니다. new , ready , running  , terminated등 있습니다.
3. 프로그램 카운터
   + 프로세스에서 실행될 다음 명령의 주소값을 가지고 있습니다.
4. CPU 레지스터
   + 프로세스가 사용중인 레지스터를 의미합니다.
5. 프로세스 권한
   + 시스템 자원에 대한 권한을 의미합니다.
6. 프로세스 구조에 대한 정보
   + 자식 프로세스는 부모프로세스로부터 복제되어 생성되는데 이때 자식 프로세스에 대한 정보를 가지고 있습니다.
7. 프로세스간에 통신을 위한 정보
   + 각각의 독립적인 프로세스간에 통신을 하는데 사용되는 다양한  플래그, 신호 , 메세지 값들을 의미합니다.
8. I/O 상태 정보
   + 프로세스에 할당된 I/O 장치 리스트들을 의미한다.
9. Accounting Information
   + 프로세스를 사용하는데있어 시간 제한 CPU 사용 시간등에 대한 정보를 의미합니다.
10. 메모리 관리정보
    + 운영체제에서 사용하는 메모리에 따라 페이지 테이블, 메모리 제한,세그먼트 테이블 정보를 의미합니다.





##### 프로세스의 상태

---

* <b>New</b> : 프로그램이 메인 메모리에 할당된 상태

* <b> Ready</b> : 할당된 프로그램이 초기화와 같은 작업을 통해 실행되기 위한 모든 준비를 마친다.

* <b>Running</b> : CPU가 해당 프로세스를 실행한다.

* <b>Waiting</b> :  프로세스가 끝나지 않은 시점에서 I/O로 인해 CPU를 사용하지 않고 다른 작업을 한다. (해당 작업이 끝나면 다시 CPU에 의해 실행되기 위해 ready 상태로 돌아가야 한다.)

* <b>Terminated</b> : 프로세스가 완전히 종료된다.

  

  ![](C:\Users\home\Desktop\공부저장\프로세스 전이도.png)

  ​																										

  ​														  [*프로세스 전이도*]

  위 그림은 <b>프로세스 상태 전이도</b>의 모습 입니다.  *new* 에서부터 프로세스 상태가 변하는지 나타납니다 *running*에서 *ready*로 변할때는 time sharing system에서 해당 프로세스가 CPU시간을 모두 소진하였을때 인터럽트에 의해 강제로 *ready*상태로 변하고, CPU는 다른 프로세스를 실행시킵니다.

  

##### 프로세스 큐

---

프로세스는 수행하면서 상태가 여러번 변하기에 서비스를 받아야 하는 곳이 다릅니다.

프로세스는 일반적으로 여러개가 한번에 수행되므로 그에 따른 순서가 필요한데

이러한 순서를 대기하는 곳을 큐라 부르고

*jOB Queue* ,*Ready Queue* ,*Device Queue* 가 있습니다.



###### Job Queue

+ 메모리 할당을 대기 중인 프로세스들로 구성 되어있습니다

###### Ready Queue

+ CPU 점유 순서를 기다리는 큐 입니다.

###### Device Queue

+ I/O를 하기 위한 여러 장치가 있는데, 각 장치를 기다리는 큐가 각각 존재합니다.

  

위와 같이 여러 큐들이 있는데 각 큐에는 저장된 실제 데이터는 각 프로세스의 **PCB**가 저장되어 있습니다. 그리고 이 순서를 기다리는 공간이 있다면 순서를 정해주는 알고리즘도 있습니다. 이러한 알고리즘을 **스케줄링 ** 이라고 부릅니다.



##### 스케줄링

---

+ Job Queue

**Job Queue**의 순서를 정해주는 스케줄을 **long-term scheduler** 라고 부릅니다.

이는 스케줄링 발생 시간이 비교적 오래걸리기 때문입니다.

+ Ready Queue

**Ready Queue**의  **short-term scheduler** 라고도 합니다.

이는 스케줄링 발생하는 시간이 매우 짧기 때문입니다. 



##### 멀티프로그래밍(Multiprogramming)

----

멀티프로그래밍은 단일프로세서 환경에서 여러개의 프로세스가 동시에 실행되는것을 말합니다(실제론X) 여러 프로세스가 실행 되려면 이 프로세스들은 모두 메인 메모리에 존재해야 합니다.

###### Degree of multiprogramming

+ Degree of multiprogramming은 현재 메모리에 할당되어 있는 프로세스 개수를 말합니다.



##### I/O bound process 

+ 해당 프로세스에서 I/O 작업 비중이 높은 프로세스를 말합니다.



##### CPU bound process

+ CPU작업 비중이 높은 프로세스를 말합니다.



**Medium-term scheduler**

+ Medium-term Scheduler는 실행되기에 필요한 자원이 부족한 프로세스들 중 어떤 프로세스를 swap-out 하여 disk에 저장할지 결정해주는 Scheduler입니다.

`Swap-out : 주메모리에서 보조장치에 위치한 Swap 영역으로 옮겨지는것을 의미합니다.`



***Long-term Scheduler*** 

+ ***Multiprogramming***의 정도를 제어합니다. ***Multiprogramming***은 한 프로세스가 CPU 작업을 끝내고 I/O 작업에 들어간다면 다른 프로세스에게 CPU를 할당해줌으로써 효율을 높이는 방법입니다.



##### Context Switching

---

**Context switching**은 CPU가 한 프로세스에서 다른 프로세스로 옯겨가는 것을 말합니다. 

즉 한 프로세스가 실행중인것을 멈추고 다른 프로세스가 실행되는것을 의미합니다.

+ ***Scheduler*** : CPU Scheduler를 말하며 CPU가 어느 프로세스를 선택할지 정합니다.

  

+ ***Dispatcher*** : 실제 Context Switching이 발생하면 CPU의 내부 데이터를 이전 프로세스 데이터에서 새로 시작되는 데이터 바꿔줍니다.

  

+ ***Context switching overhead*** : Context switching이 발생할 때마다, dispatcher에서 수행하는 작업을 매번 수행해야하며 이 모든것이 overhead입니다. ***Context switching***은 매우 자주 발생하는 작업이므로 overhead를 줄이기 위해 dispatcher를 구현하는 코드에 효울을 높여 줘야 합니다.

##  인라인뷰

* 인라인뷰란?

  * **from** 절에 나오는 **select**를 말한다. 

    * from 절 뒤에는 원래 table이 나와야하는데 그자리에 select가 나오는경우 select를 table처럼 본다.

    * 인라인뷰는 select 결과물 테이블로 취급하기 때문에 메모리에 부담을준다.

      

    ```sql
    select
        rownum , dep_no , dep_name , loc
        from dept
    ```

  

  * **rownum** : 행의 번호대로 테이블에 적어줍니다.

  * 오라클은 모든 select 결과물에 rownum 컬럼을 붙여준다. 이 컬럼안에는 1부터 시작하는 일련번호가 들어가 있다.

  * rownum 컬럼이 where 절에 들어갈 때 주의사항

    * order by 가 있을 경우 정렬된 후에 rownum 컬럼이 붙는게 아니라 정렬되기전에 rownum 컬럼이 붙고 난 후 정렬이 된다. 그러므로 rownum컬럼에 일련번호가 뒤엉켜 버린다.
    * rownum 자리에 1이 들어가서 false 가 나오는 조건이 있다면 조건이 성립되지 않는다.
    * **EX)** rownum<=3 (0) rownum=1(0)  rownum>=3(x)  rownum>1(x) rownum = 2(x) 
    * rownum >=1 and rownum <= 3 (0)
    * rownum >=2 and rownum <= 3 (0)

  * 인라인 뷰 , 즉 from 절에 나오는 select 는 바깥쪽 select 입장에서는 테이블 처럼 생각한다. 인라인뷰안에 rownum 컬럼이 있다면 바깥쪽 select 입장에서는 안쪽 rownum 컬럼은 일반 컬럼으로 생각한다.

    ---

    

  > ### 참고 문제 n행 ~ m행까지 검색 이유 [면]

  * 실제로는 select 결과물이 웹브라우저로 출력된다
  * 만약 select 결과물이 10만행이면 다 보여줄경우 웹브라우저 로딩이 걸린다.
  * 그러므로 1~10행 또는 1행~15행씩 끊어서 웹브라우저로 출력되야 로딩이 걸리지 않는다. 이렇게 sleect 결과물에서 n행씩 끊어서 화면에 보여주는것을 **[페이징]** 처리 라고 부른다.



---

> ### n행~m행 까지 검색하는 select 패턴 정리1

```sql
select *from (select rownum RNUM , 별칭. * from(
원하는 정렬 select 문) 별칭 where rownum<=마지막 행 번호) where RNUM>=시작 행 번호
```

> ### n행~m행 까지 검색하는 select 패턴 정리2

* 아래 방법이 2배 정도 느립니다.

```sql
select * from (select rownum RNUM , 별칭 .* from(원하는 정렬 select 문) 별칭 ) where RNUM >= 시작행 번호 and RNUM <= 마지막 행번호
```



* 이렇게도 가능하지만 속도 차이가 많이 납니다. 2배정도 느립니다.

```sql
SELECT * 
  FROM
       (SELECT ROWNUM RNUM,
              e.* 
         FROM
              (SELECT * 
                FROM employee 
            ORDER BY salary desc
              ) e 
      
       )
 WHERE RNUM>=1 and RNUM<=10

```



* 문법

```sql
SELECT * 
  FROM 
       (SELECT ROWNUM RNUM , 
              별칭.* 
         FROM( 원하는 select문) 별칭 
        WHERE ROWNUM<= 마지막 행번호
       ) 
 WHERE RNUM >=시작행번호
```

* 문법 활용

```sql
SELECT * 
  FROM 
       (SELECT ROWNUM RNUM , 
              j.* 
         FROM
              (SELECT * 
                FROM customer 
            ORDER BY decode(substr(jumin_num,7,1),'1','19','2','19','20')||substr(jumin_num,1,6) ASC    
              ) j 
        WHERE ROWNUM<= 10 
       ) 
 WHERE RNUM >=6
```



---

> ### 연봉서열대로 직원을 검색하되 1행부터 10행까지만 검색하면?

```sql
SELECT * 
  FROM employee 
 WHERE ROWNUM<=10 
ORDER BY salary desc
```

* 정렬 전에 rownum을 붙여 정렬 전에 정렬하여 rownum 일련번호가 깨져서 보인다.



```sql
SELECT * 
  FROM
       (SELECT ROWNUM RNUM,
              e.* 
         FROM
              (SELECT * 
                FROM employee 
            ORDER BY salary desc
              ) e 
        WHERE ROWNUM<=10
       )
 WHERE RNUM>=1

```

* 인라인뷰 from 절안에 table 대신 select를 적어 rownum이 섞이지 않게 할수 있다.
* 정렬후 정렬 가능



> ### 연봉서열대로 직원을 검색하되 11행부터 20행까지만 검색하면?

```sql
select  * from (SELECT rownum RNUM,       e.*   FROM        (SELECT *          FROM employee      ORDER BY salary desc       ) e  WHERE     rownum <=20)    where RNUM >=11
```

* 인라인뷰를 안쪽에 만들고 from 절안에 넣어 테이블로 두어서 바깥에 where 절로 다시 한번 정렬합니다.



> ### 직원 직급 서열중 2행~5행 까지 검색하면?

```sql
SELECT *   FROM       (SELECT ROWNUM RNUM ,               j.*         FROM               (SELECT *                 FROM employee             ORDER BY decode(jikup,'사장',1,'부장',2,'과장',3,'대리',4,'주임',5,6) ASC              )j         WHERE ROWNUM<=5       ) WHERE RNUM>=2;
```



> ### 월별로 [입사월] [인원수] 를 검색
>
> 조건? 1~12월 모두 출력

```sql
select    m.month||'월' "입사월"    ,count(e.emp_name) "입사인원수"    from(    select '01' "MONTH" from dual union    select '02' from dual union select '03' from dual union select '04' from dual union select '05' from dual union    select '06' from dual union select '07' from dual union select '08' from dual union select '09' from dual union    select '10' from dual union select '11' from dual union select '12' from dual    )m    ,employee e    where    to_char(e.hire_date(+),'mm')=m.month    group by m.month||'월'    order by "입사월"
```





> ### 오늘부터 10일 이후까지 날짜중에 토요일 일요일 월요일을 제외한 날의 개수

```sql
select count(*) from(select sysdate "XDAY" from dualunion select sysdate+1 from dual union select sysdate+2 from dualunion select sysdate+3 from dual union select sysdate+4 from dualunion select sysdate+5 from dual union select sysdate+6 from dualunion select sysdate+7 from dual union select sysdate+8 from dualunion select sysdate+9 from dual union select sysdate+10 from dual) dwhereto_char(d.xday,'dy','nls_date_language=korean')!='토'and to_char(d.xday,'dy','nls_date_language=korean')!='일'and to_char(d.xday,'dy','nls_date_language=korean')!='월'
```



> ### 이번달 중에 토요일 , 일요일을 제외한 날의 개수를 구하면?
>
> 이번달 평일 수 구하기

```sql
SELECT count(*)   FROM       (SELECT to_date(to_char(sysdate,'yyyy-mm')||'-01','yyyy-mm-dd')+RNUM-1"XDAY"          FROM               (SELECT ROWNUM RNUM                 FROM employee                   UNION               SELECT ROWNUM+20                 FROM employee              )         WHERE RNUM <= to_number(to_char(last_day(sysdate),'dd'))       ) d  WHERE to_char(d.XDAY , 'dy' , 'nls_date_language =korean')!='토'        AND to_char(d.XDAY, 'dy','nls_date_language=korean')!='일'        AND XDAY <=last_day(sysdate)
```

