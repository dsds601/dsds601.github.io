---
layout: post
title:  "메모리구조"
summary: "메모리구조"
author: GH
date: '2021-05-21 14:35:23 +0530'
category: 프로그래밍 기초
thumbnail: /i.imgur.com/FdJ8Xbd.png
keywords: devlopr jekyll, how to use devlopr, devlopr, how to use devlopr-jekyll, devlopr-jekyll tutorial,best jekyll themes
permalink: /blog/categories/프로그래밍 기초/
usemathjax: true

---
# 메모리 구조

프로그램이 실행되기 위해서 프로그램이 메모리에 로드되어야 합니다.

또한 프로그램에서는 변수들을 저장할 메모리도 필요합니다.

따라서 운영체제는 프로그램의 실행을 위한 다양한 메모리 공간을 제공하고 있는데

대표적인 메모리 공간으로는 아래와 같습니다

1. **코드(code)영역**
2. **데이터(data)영역**
3. **힙(heap)영역**
4. **스택(stack)영역**

![](https://i.imgur.com/FdJ8Xbd.png)

- 이미지 출처 :https://jinshine.github.io/2018/05/17/%EC%BB%B4%ED%93%A8%ED%84%B0%20%EA%B8%B0%EC%B4%88/%EB%A9%94%EB%AA%A8%EB%A6%AC%EA%B5%AC%EC%A1%B0/

<hr/>

+ ###  데이터 영역

  + 코드(Text)영역은 ***실행할 프로그램에 코드*** (프로그래머가 작성한 소스코드) 가 저장되는 영역입니다. 프로그램이 시작하고 끝날때까지 메모리에 계속 남아 있습니다.  실제 연산하는 ***CPU는 코드영역에 저장되어 있는 명령어를 가져다 처리를 합니다.*** 즉, 코드 영역은 컴퓨터가 이해할 수 있는 ***기계어로 번역된 소스코드가 올라가는 공간*** 이며 상수도 여기에 들어갑니다.  



---

+ ### 데이터 영역

  + 메모리의 데이터영역은 프로그램의 ***전역 변수와 정적(static) 변수가 저장되는 영역***입니다.

    프로그램이 시작하며 메모리에 적재되고 종료가되면 사라지는데 이는 곧 전역변수와 정적변수는 프로그램이 종료될때까지

    계속 남아서 다른 함수에서도 사용할 수 있는 이유가 됩니다.

---

+ ### 스택 영역

  + 메모리의 스택 영역은 함수의 호출과 관계되는 ***지역변수와 매개변수***가 저장되는 공간입니다.

    스택 영역은 함수가 호출할때 할당되며 호출이 완료되면 데이터는 사라지게 됩니다.

    스택영역은 박스가 쌓이듯이 먼저 들어오는 순서대로 저장이되며 호출시에는 마지막에 들어오는 데이터가 먼저 호출 됩니다(Last In First Out), 이렇게 메모리도 높은 주소에서 낮은 주소의 방향으로 할당 되게 됩니다.

    

    스택영역에 저장되 있는 함수의 호출 정보를 Stack Frame이라고 부르며, 프로그램에서 현재 실행중인 함수에 대한 정보를

    저장하는 스택 자료구조를 Call Stack 이라고 부릅니다.

    Stack 영역이 클수록 Heap영역이 작아지고 반대로 Heap 영역이 클수록 Stack 영역이 작아지게 되는데 스택 영역은 컴파일시 크기가 결정되게 됩니다.

---

+ ### 힙 영역

  + 힙 영역은 사용자에 의해 프로그램이 진행시 메모리를 사용할 수 있는 영역입니다. 사용자가 원하는 시점에 할당하고 소멸할수있또록 변수들이 할당되는 영역입니다.

    스택에서 포인터 변수를 할당하고 포인터가 가리키는 Heap 영역의 임의공간으로부터 동적 할당한 크기만큼 사용합니다.

    메모리를 사용하고 난 후에는 반드시 메모리 반환을 해줘야하는데 C,C++ 같은 경우에는 개발자가 직접해야 하지만 JAVA같은 경우에는 Garbage Collection이 개발자 대신 해줍니다.

---

**스택영역과 힙영역에 빈공간**

이미지에 빈 공간은 **Stack** 과 **Heap** 은 여유공간을 위해 지원합니다.

Stack의 지역변수는 사용되고 소멸되기 때문에 용량의 불확실성 때문에 밑에서부터 올라가게 되는데, 이때 Stack영역이 Heap영역으로 침범하는 경우를 **Stack overflow**라 부릅니다.

Heap도 같은이유로 위에서부터 채워가게 되는데 마찬가지로 Heap도 Stack영역으로 침범하는 경우를 **Heap overflow**라고 부릅니다.




# 메모리

***메모리*** 는 **주소와 데이터**로 구성이 되어있다



1. 주소공간

   + 프로세스에 참조할 수 있는 주소들의 범위를 의미한다. 

     프로세스와 1:1 관계를 가지며 사용자 쓰레드는 주소공간을 공유한다.

     주소공간의 크기는 CPU의 주소버스에 의존을 하며 CPU는 컴퓨터에 메모리에 설치되며 각 메모리의 주소 공간이 있다. 이렇게 설치된 메모리의 주소공간을 **물리 주소 공간**이라고 한다. 물리 주소 공간은 하드웨어 입장에서 바라본 공간으로 컴퓨터마다 그 크기가 다르다. 이와 반대로 사용자 입장에서 바라본 주소 공간은 **논리 주소 공간**이라고 한다.

2. 물리 주소

   + 컴퓨터의 메인 메모리를 접근할 때 사용되는 주소를 물리 주소 라고 한다. 이는 메모리 관리자 입장에서 바라본 주소이다. 메모리에서 어떤 값을 읽어내기에는 편하지만 운영체제가 업그레이드 되면 주소 범위가 바뀔수 있기에 사용자 입장에서는 불편할 수 있다.

3. 가상 주소

   + 물리주소는 사용자입장에서 범위가 바뀌면 불편할 수 있기에 프로세스 입장에서 사용자 주소를 따로 정의하는데 이를 가상주소 라고 한다. 물리주소와 관계없이 0번지에서 시작한다.



CPU는 레지스터를 참조하며 메모리 공간을 보호하고 레지스터 정보는 PCB에 담겨있다.

개별적인 메모리 공간을 분리하고 프로세스가 합법적인 영역만 접근 하도록 하는것이 필요하다. 이때 레지스터는 base와 limit으로 나눠 보호 기법을 제공한다

base는 프로세스가 합법적인 물리 메모리 주소의 값을 저장하고 limit는 주어진 영역의 크기를 저장한다. 즉 프로세스가 사용할 수 있는 가장 큰 주소는 base와 limit의 합이된다.





![memory](https://t1.daumcdn.net/cfile/tistory/99B660425BAB4FEC21)

- 이미지 출처 : https://teraphonia.tistory.com/791

## 주소 할당 (Address Binding)

CPU가 명령을 수행하면  data를 메모리 어느 번지에 들어갈지 결정하는데 이걸 **주소할당**이라고 부릅니다. 주소를 할당하는데에는 크게 3단계가 있습니다.

1. **compile time**

   + 캄파일 할 때 바인딩이 이루어집니다. 물리적인 메모리가 많이 비었어도 주소가 결정 되었으면 변경할 수 없는 비효율적인 방법으로 주소를 변경할수 없으므로 컴파일된 코드를 absolute code라고 부릅니다.

2. **load time**

   + 프로그램이 실행되었을때 할당이 이루어지며 컴파일러가 재배치 가능한 코드라 하여서 relocatable code라고 부릅니다.

3. **execution time**

   + 현재 사용하는 방법으로 프로그램이 실행된 후에도 주소를 변경 가능합니다.

     CPU가 주소를 참조할 때마다 새로 할당 상태를 점검해야  하므로 MMU같은 하드웨어 적인 지원이 필요합니다 

![addressbinding](https://steemitimages.com/640x0/https://cdn.steemitimages.com/DQmT46bmffdyBRLTJUAMpK5gmxZcVrCNYLKGS14NN6JYCNE/Capture.PNG)

- 이미지 출처: https://steemit.com/operatingsystem/@sapphire317/address-binding



### MMU (Memory Management Unit)

---



![MMU](https://img1.daumcdn.net/thumb/R1280x0/?scode=mtistory2&fname=https%3A%2F%2Fblog.kakaocdn.net%2Fdn%2FbJDuGg%2FbtqxC7uysgv%2Fu6kxrknlZw494q7w8uz6K0%2Fimg.png)

- 이미지 출처:https://dely.tistory.com/78

CPU에서 논리주소를 사용하는 경우 CPU와 Memory 사이에서 Translation이 일어나게 되는데 이 Translation을 수행하는 장치를 MMU(Memory Management Unit)이라고 합니다.

CPU로부터 받은 가상주소를 물리주소로 번역하여 메모리에 보내는 역활을 합니다.
